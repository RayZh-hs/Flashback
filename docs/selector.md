# Selector

A selector is a text field that allows users to specify a selection of objects in the world. Different types of selectors target different types of objects.

## Block Selector

Block selectors build on top of the block selection syntax.

0,0,0,7,7,7[type=air]:8,8,8